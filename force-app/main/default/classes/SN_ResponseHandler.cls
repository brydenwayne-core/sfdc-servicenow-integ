public with sharing class SN_ResponseHandler {
    /** Interprets the ServiceNow response and maps it into transaction, linkage, and source-record update outcomes. */
    public static void handleIncidentResponse(Id transactionId, HttpResponse response) {
    }
}
